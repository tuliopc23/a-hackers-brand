---
title: 'Progress'
description: 'Animated progress bars with neon effects and terminal styling'
---

# Progress

Cyberpunk-styled progress indicators that show system status, loading states, and data transfer progress with authentic terminal aesthetics.

## Import

```svelte
import { Progress } from 'bubble-tea-party'
```

## Basic Usage

```svelte
<script>
  import { Progress } from 'bubble-tea-party'
  
  let progress = 67
</script>

<Progress value={progress} variant="cyberpunk" />
```

## Variants

<Tabs>
  <Tab title="Default">
    ```svelte
    <Progress value={45} />
    ```
  </Tab>
  
  <Tab title="Cyberpunk">
    ```svelte
    <Progress value={78} variant="cyberpunk" />
    ```
  </Tab>
  
  <Tab title="Neon">
    ```svelte
    <Progress value={91} variant="neon" color="cyan" />
    ```
  </Tab>
  
  <Tab title="Terminal">
    ```svelte
    <Progress value={34} variant="terminal" />
    ```
  </Tab>
</Tabs>

## Props

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `value` | `number` | `0` | Progress value (0-100) |
| `variant` | `'default' \| 'cyberpunk' \| 'neon' \| 'terminal'` | `'default'` | Style variant |
| `color` | `string` | `'green'` | Neon color for effects |
| `showLabel` | `boolean` | `true` | Show percentage label |
| `animated` | `boolean` | `true` | Enable animations |

---

Show progress with cyberpunk style! 📊⚡