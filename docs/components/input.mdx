---
title: 'Input'
description: 'Cyberpunk-styled input fields with terminal aesthetics and validation'
---

# Input

Terminal-inspired input fields that bring authentic cyberpunk aesthetics to your forms. Features neon glows, typing animations, and comprehensive validation.

## Import

```svelte
import { Input } from 'bubble-tea-party'
```

## Basic Usage

<CodeGroup>

```svelte Default
<script>
  import { Input } from 'bubble-tea-party'
  
  let username = ''
</script>

<Input 
  label="Username"
  bind:value={username}
  placeholder="Enter your handle..."
/>
```

```svelte Cyberpunk
<Input 
  label="Access Code"
  variant="cyberpunk"
  type="password"
  placeholder="••••••••"
/>
```

```svelte Terminal
<Input 
  label="Command"
  variant="terminal"
  placeholder="root@matrix:~$ "
/>
```

</CodeGroup>

<iframe 
  src="/playground/input-basic" 
  width="100%" 
  height="400"
  className="border border-green-500 rounded-lg mb-6"
/>

## Variants

<Tabs>
  <Tab title="Default">
    Clean input with subtle cyberpunk styling.
    ```svelte
    <Input label="Standard Input" />
    ```
  </Tab>
  
  <Tab title="Cyberpunk">
    Full neon treatment with glowing borders.
    ```svelte
    <Input label="Cyber Input" variant="cyberpunk" />
    ```
  </Tab>
  
  <Tab title="Terminal">
    Terminal-style with monospace font and prompt.
    ```svelte
    <Input label="Terminal" variant="terminal" />
    ```
  </Tab>
  
  <Tab title="Matrix">
    Matrix rain background with green glow.
    ```svelte
    <Input label="Matrix Mode" variant="matrix" />
    ```
  </Tab>
</Tabs>

## Input Types

<CardGroup cols={2}>
  <Card title="Text">
    ```svelte
    <Input type="text" label="Username" />
    ```
  </Card>
  <Card title="Password">
    ```svelte
    <Input type="password" label="Access Key" />
    ```
  </Card>
  <Card title="Email">
    ```svelte
    <Input type="email" label="Neural Link" />
    ```
  </Card>
  <Card title="Number">
    ```svelte
    <Input type="number" label="Port" />
    ```
  </Card>
</CardGroup>

## Validation & States

### Built-in Validation

```svelte
<script>
  import { Input } from 'bubble-tea-party'
  
  let email = ''
  let isValid = false
  
  function validateEmail(value) {
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)
  }
  
  $: isValid = validateEmail(email)
</script>

<Input 
  bind:value={email}
  label="Email Address"
  type="email"
  validation={{
    required: true,
    pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    message: 'Please enter a valid email address'
  }}
  error={!isValid && email.length > 0}
  success={isValid && email.length > 0}
/>
```

### Error States

```svelte
<Input 
  label="Username"
  error={true}
  errorMessage="Username already exists in the matrix"
  variant="cyberpunk"
/>
```

### Success States

```svelte
<Input 
  label="Access Granted"
  success={true}
  successMessage="Connection established"
  variant="cyberpunk"
/>
```

## Advanced Features

### With Icons

```svelte
<script>
  import { Input, Icon } from 'bubble-tea-party'
</script>

<Input 
  label="Search Matrix"
  leftIcon="search"
  rightIcon="terminal"
  placeholder="Find what you're looking for..."
/>
```

### Character Counter

```svelte
<Input 
  label="Bio Data"
  maxLength={140}
  showCounter={true}
  variant="cyberpunk"
  placeholder="Describe your digital identity..."
/>
```

### Auto-complete

```svelte
<script>
  let suggestions = [
    'hack-the-gibson',
    'neo@matrix.com', 
    'morpheus-guide',
    'trinity-backup'
  ]
</script>

<Input 
  label="Handle"
  suggestions={suggestions}
  variant="terminal"
/>
```

## Interactive Demo

<iframe 
  src="/playground/input-demo" 
  width="100%" 
  height="600"
  className="border border-green-500 rounded-lg mb-6"
/>

## Props

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `variant` | `'default' \| 'cyberpunk' \| 'terminal' \| 'matrix'` | `'default'` | Input style variant |
| `type` | `string` | `'text'` | HTML input type |
| `label` | `string` | `''` | Input label text |
| `placeholder` | `string` | `''` | Placeholder text |
| `value` | `string` | `''` | Input value |
| `disabled` | `boolean` | `false` | Disable input |
| `readonly` | `boolean` | `false` | Make input readonly |
| `required` | `boolean` | `false` | Mark as required |
| `error` | `boolean` | `false` | Show error state |
| `success` | `boolean` | `false` | Show success state |
| `errorMessage` | `string` | `''` | Error message text |
| `successMessage` | `string` | `''` | Success message text |
| `leftIcon` | `string` | `''` | Left icon name |
| `rightIcon` | `string` | `''` | Right icon name |
| `maxLength` | `number` | `undefined` | Maximum character length |
| `showCounter` | `boolean` | `false` | Show character counter |
| `suggestions` | `string[]` | `[]` | Auto-complete suggestions |

## Events

| Event | Description |
|-------|-------------|
| `input` | Fired on value change |
| `focus` | Fired when input gains focus |
| `blur` | Fired when input loses focus |
| `keydown` | Fired on key press |
| `keyup` | Fired on key release |
| `paste` | Fired when content is pasted |

## Styling

### CSS Variables

```css
:root {
  --input-border-radius: 4px;
  --input-border-width: 1px;
  --input-padding: 12px 16px;
  --input-font-size: 14px;
  --input-line-height: 1.5;
  --input-transition: all 0.3s ease;
  --input-neon-glow: 0 0 10px var(--neon-green);
}
```

### Custom Themes

```svelte
<Input 
  class="custom-input"
  style="--neon-color: #ff6b35; --bg-opacity: 0.1;"
  label="Custom Theme"
/>

<style>
  :global(.custom-input) {
    --input-font-family: 'Orbitron', monospace;
    --input-text-transform: uppercase;
    --input-letter-spacing: 0.05em;
  }
</style>
```

## Accessibility

- Full keyboard navigation
- Screen reader support with proper labels
- ARIA states for validation
- High contrast mode compatibility
- Focus management and indicators

## Form Integration

### With Form Validation

```svelte
<script>
  import { Input, Button, FormErrorBoundary } from 'bubble-tea-party'
  
  let formData = {
    username: '',
    email: '',
    password: ''
  }
  
  let errors = {}
  
  function validateForm() {
    errors = {}
    
    if (!formData.username) {
      errors.username = 'Username is required'
    }
    
    if (!formData.email.includes('@')) {
      errors.email = 'Valid email required'
    }
    
    if (formData.password.length < 8) {
      errors.password = 'Password must be 8+ characters'
    }
    
    return Object.keys(errors).length === 0
  }
  
  function handleSubmit() {
    if (validateForm()) {
      console.log('Access granted:', formData)
    }
  }
</script>

<FormErrorBoundary>
  <div class="cyber-form">
    <Input 
      bind:value={formData.username}
      label="Handle"
      variant="cyberpunk"
      error={!!errors.username}
      errorMessage={errors.username}
      required
    />
    
    <Input 
      bind:value={formData.email}
      label="Neural Link"
      type="email"
      variant="cyberpunk"
      error={!!errors.email}
      errorMessage={errors.email}
      required
    />
    
    <Input 
      bind:value={formData.password}
      label="Access Code"
      type="password"
      variant="cyberpunk"
      error={!!errors.password}
      errorMessage={errors.password}
      required
    />
    
    <Button 
      variant="neon" 
      onClick={handleSubmit}
      class="mt-4"
    >
      Jack In
    </Button>
  </div>
</FormErrorBoundary>
```

## Best Practices

<Tip>
Use the `terminal` variant for code inputs and command interfaces to maintain the cyberpunk aesthetic.
</Tip>

<Warning>
Always provide clear error messages - in the matrix, unclear feedback can be fatal.
</Warning>

<Info>
The input automatically adjusts its glow intensity based on focus state and validation status.
</Info>

---

Ready to collect user data like a true cyber-detective? The Input component is your digital interrogation tool! 🔍⚡