---
title: 'Playground'
description: 'Interactive component playground and examples'
---

# Playground

Experiment with Bubble Tea Party components in real-time. Mix and match different combinations to create the perfect cyberpunk interface.

## Interactive Component Builder

<iframe 
  src="/playground/component-builder" 
  width="100%" 
  height="700"
  className="border border-green-500 rounded-lg mb-8"
/>

## Featured Examples

<CardGroup cols={2}>
  <Card title="Terminal Dashboard" icon="terminal">
    Complete dashboard with terminal windows, metrics, and real-time data
    <iframe src="/playground/terminal-dashboard" height="300" />
  </Card>
  
  <Card title="Cyberpunk Form" icon="user">
    Registration form with validation and neon effects
    <iframe src="/playground/cyberpunk-form" height="300" />
  </Card>
  
  <Card title="Matrix Interface" icon="grid">
    Data grid with matrix rain background and interactive elements
    <iframe src="/playground/matrix-interface" height="300" />
  </Card>
  
  <Card title="Neon Navigation" icon="navigation">
    Navigation system with glowing buttons and smooth transitions
    <iframe src="/playground/neon-navigation" height="300" />
  </Card>
</CardGroup>

## Code Examples

### Complete Terminal Interface

```svelte
<script>
  import { 
    TerminalWindow, 
    AdvancedTerminal, 
    MatrixRain,
    Button,
    StatusBar 
  } from '@org/ui'
  
  let commands = []
  
  function handleCommand(cmd) {
    commands = [...commands, {
      command: cmd,
      output: `Executing: ${cmd}`,
      timestamp: new Date()
    }]
  }
</script>

<div class="cyber-interface">
  <MatrixRain />
  
  <StatusBar status="online" />
  
  <TerminalWindow title="SYSTEM CONTROL">
    <AdvancedTerminal 
      prompt="neo@matrix:~$"
      {commands}
      onCommand={handleCommand}
      aiEnabled={true}
    />
  </TerminalWindow>
  
  <div class="actions">
    <Button variant="neon">Execute</Button>
    <Button variant="cyberpunk">Deploy</Button>
  </div>
</div>
```

### Cyberpunk Dashboard

```svelte
<script>
  import { 
    Card, 
    MetricsCard, 
    Progress, 
    DataTable,
    GlowText 
  } from '@org/ui'
  
  let metrics = {
    cpuUsage: 87,
    memoryUsage: 64,
    networkTraffic: 92
  }
</script>

<div class="dashboard">
  <header>
    <GlowText size="xl" color="cyan">
      CYBER COMMAND CENTER
    </GlowText>
  </header>
  
  <div class="metrics-grid">
    <MetricsCard 
      title="CPU Usage"
      value="{metrics.cpuUsage}%"
      trend="up"
      variant="cyberpunk"
    />
    
    <MetricsCard 
      title="Memory"
      value="{metrics.memoryUsage}%"
      trend="stable"
      variant="neon"
    />
    
    <MetricsCard 
      title="Network"
      value="{metrics.networkTraffic}%"
      trend="down"
      variant="cyberpunk"
    />
  </div>
  
  <Card variant="glass">
    <h3>System Performance</h3>
    <Progress value={metrics.cpuUsage} variant="neon" />
    <Progress value={metrics.memoryUsage} variant="cyberpunk" />
    <Progress value={metrics.networkTraffic} variant="terminal" />
  </Card>
</div>
```

## Try It Yourself

<Tabs>
  <Tab title="Components">
    <iframe 
      src="/playground/components" 
      width="100%" 
      height="500"
    />
  </Tab>
  
  <Tab title="Themes">
    <iframe 
      src="/playground/themes" 
      width="100%" 
      height="500"
    />
  </Tab>
  
  <Tab title="Effects">
    <iframe 
      src="/playground/effects" 
      width="100%" 
      height="500"
    />
  </Tab>
  
  <Tab title="Templates">
    <iframe 
      src="/playground/templates" 
      width="100%" 
      height="500"
    />
  </Tab>
</Tabs>

## Component Combinations

### Form + Validation

```svelte
<script>
  import { 
    Input, 
    Button, 
    FormErrorBoundary,
    Toast 
  } from '@org/ui'
</script>

<FormErrorBoundary>
  <form class="cyber-form">
    <Input 
      label="Access Code"
      type="password"
      variant="cyberpunk"
      required
    />
    
    <Input 
      label="Neural Link ID"
      variant="terminal"
      required
    />
    
    <Button variant="neon" type="submit">
      Jack In
    </Button>
  </form>
</FormErrorBoundary>
```

### Data Display

```svelte
<script>
  import { 
    DataTable, 
    Badge, 
    Progress 
  } from '@org/ui'
  
  let data = [
    { id: 1, status: 'active', progress: 87 },
    { id: 2, status: 'offline', progress: 23 },
    { id: 3, status: 'error', progress: 0 }
  ]
</script>

<DataTable 
  {data}
  columns={[
    { key: 'id', title: 'ID' },
    { 
      key: 'status', 
      title: 'Status',
      render: (value) => `<Badge variant="${value}">${value}</Badge>`
    },
    {
      key: 'progress',
      title: 'Progress',
      render: (value) => `<Progress value="${value}" variant="neon" />`
    }
  ]}
  variant="cyberpunk"
/>
```

## Advanced Patterns

### Modal + Terminal

```svelte
<script>
  import { 
    Modal, 
    AdvancedTerminal,
    Button 
  } from '@org/ui'
  
  let showTerminal = false
</script>

<Button onClick={() => showTerminal = true}>
  Open Terminal
</Button>

<Modal bind:open={showTerminal} variant="terminal" size="lg">
  <AdvancedTerminal 
    prompt="root@matrix:~$"
    aiEnabled={true}
    theme="matrix"
  />
</Modal>
```

## Tips & Tricks

<Tip>
Combine `MatrixRain` with glass variant components for an authentic cyberpunk layered effect.
</Tip>

<Warning>
Don't overuse neon effects - balance bright components with darker elements for better visual hierarchy.
</Warning>

<Info>
All playground examples are fully responsive and work great on mobile devices.
</Info>

---

Ready to build the future? Start experimenting in the playground! 🚀⚡