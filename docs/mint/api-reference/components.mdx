---
title: 'Components API'
description: 'Complete API reference for all Bubble Tea Party components'
---

# Components API Reference

Comprehensive API documentation for all Bubble Tea Party components. Each component includes props, events, slots, and usage examples.

## Core Components

### Button

Interactive cyberpunk-styled buttons with multiple variants and effects.

<Fields>
  <Field name="variant" type="string" default="default">
    Button style variant: `default`, `cyberpunk`, `neon`, `ghost`, `glitch`
  </Field>
  <Field name="size" type="string" default="md">
    Button size: `sm`, `md`, `lg`
  </Field>
  <Field name="color" type="string" default="green">
    Neon glow color for neon variant
  </Field>
  <Field name="disabled" type="boolean" default="false">
    Disable button interaction
  </Field>
  <Field name="loading" type="boolean" default="false">
    Show loading state with spinner
  </Field>
  <Field name="href" type="string" default="undefined">
    If provided, renders as anchor link
  </Field>
</Fields>

**Events:**
- `click` - Fired when button is clicked
- `focus` - Fired when button receives focus
- `blur` - Fired when button loses focus

---

### Input

Terminal-inspired input fields with validation and cyberpunk styling.

<Fields>
  <Field name="variant" type="string" default="default">
    Input style: `default`, `cyberpunk`, `terminal`, `matrix`
  </Field>
  <Field name="type" type="string" default="text">
    HTML input type
  </Field>
  <Field name="label" type="string" default="">
    Input label text
  </Field>
  <Field name="placeholder" type="string" default="">
    Placeholder text
  </Field>
  <Field name="value" type="string" default="">
    Input value (bindable)
  </Field>
  <Field name="error" type="boolean" default="false">
    Show error state
  </Field>
  <Field name="errorMessage" type="string" default="">
    Error message text
  </Field>
</Fields>

---

### Card

Container component with cyberpunk styling and glass effects.

<Fields>
  <Field name="variant" type="string" default="default">
    Card style: `default`, `cyberpunk`, `glass`, `neon`
  </Field>
  <Field name="padding" type="string" default="md">
    Internal padding: `sm`, `md`, `lg`
  </Field>
  <Field name="hoverable" type="boolean" default="false">
    Enable hover effects
  </Field>
  <Field name="clickable" type="boolean" default="false">
    Make card clickable
  </Field>
</Fields>

---

## Terminal Components

### AdvancedTerminal

Full-featured terminal emulator with command history and AI suggestions.

<Fields>
  <Field name="prompt" type="string" default="$">
    Terminal prompt text
  </Field>
  <Field name="commands" type="Command[]" default="[]">
    Array of command history objects
  </Field>
  <Field name="suggestions" type="string[]" default="[]">
    Available command suggestions
  </Field>
  <Field name="aiEnabled" type="boolean" default="false">
    Enable AI-powered command suggestions
  </Field>
  <Field name="theme" type="string" default="matrix">
    Terminal color theme: `matrix`, `amber`, `green`
  </Field>
</Fields>

**Events:**
- `command` - Fired when command is executed
- `suggestion` - Fired when suggestion is selected

---

### MatrixRain

Animated matrix digital rain background effect.

<Fields>
  <Field name="speed" type="number" default="30">
    Animation speed (higher = faster)
  </Field>
  <Field name="density" type="number" default="0.6">
    Character density (0-1)
  </Field>
  <Field name="color" type="string" default="#00ff41">
    Rain color
  </Field>
  <Field name="characters" type="string" default="01">
    Characters to use in animation
  </Field>
  <Field name="fadeEnabled" type="boolean" default="true">
    Enable fade effect
  </Field>
</Fields>

---

## Data Components

### DataTable

Cyberpunk-styled data table with sorting and filtering.

<Fields>
  <Field name="data" type="any[]" default="[]">
    Array of data objects
  </Field>
  <Field name="columns" type="Column[]" default="[]">
    Column configuration
  </Field>
  <Field name="variant" type="string" default="default">
    Table style: `default`, `cyberpunk`, `neon`
  </Field>
  <Field name="sortable" type="boolean" default="true">
    Enable column sorting
  </Field>
  <Field name="filterable" type="boolean" default="true">
    Enable data filtering
  </Field>
</Fields>

---

### Data Grid

High-performance virtualized grid for thousands of rows.

<Fields>
  <Field name="columns" type="Column[]" default="[]">
    Column definitions with widths (px)
  </Field>
  <Field name="rows" type="any[]" default="[]">
    Row data objects
  </Field>
  <Field name="rowHeight" type="number" default="28">
    Row height in pixels
  </Field>
</Fields>

---

### Table

Semantic table with sticky header and terminal borders.

<Fields>
  <Field name="columns" type="Column[]" default="[]">
    Column configuration
  </Field>
  <Field name="rows" type="any[]" default="[]">
    Row data
  </Field>
</Fields>

---

### Progress

Animated progress bars with neon effects.

<Fields>
  <Field name="value" type="number" default="0">
    Progress value (0-100)
  </Field>
  <Field name="variant" type="string" default="default">
    Progress style: `default`, `cyberpunk`, `neon`, `terminal`
  </Field>
  <Field name="color" type="string" default="green">
    Neon color for effects
  </Field>
  <Field name="showLabel" type="boolean" default="true">
    Show percentage label
  </Field>
  <Field name="animated" type="boolean" default="true">
    Enable animations
  </Field>
</Fields>

---

## Effect Components

### GlowText

Text with customizable neon glow effects.

<Fields>
  <Field name="color" type="string" default="green">
    Glow color
  </Field>
  <Field name="size" type="string" default="md">
    Text size: `sm`, `md`, `lg`, `xl`
  </Field>
  <Field name="animated" type="boolean" default="false">
    Enable glow animation
  </Field>
  <Field name="pulse" type="boolean" default="false">
    Pulsing effect
  </Field>
  <Field name="flicker" type="boolean" default="false">
    Flicker effect
  </Field>
  <Field name="intensity" type="string" default="medium">
    Glow intensity: `low`, `medium`, `high`
  </Field>
</Fields>

---

## Layout Components

### Modal

Overlay dialogs with cyberpunk styling and animations.

<Fields>
  <Field name="open" type="boolean" default="false">
    Modal visibility state (bindable)
  </Field>
  <Field name="title" type="string" default="">
    Modal header title
  </Field>
  <Field name="variant" type="string" default="default">
    Modal style: `default`, `cyberpunk`, `terminal`, `glass`
  </Field>
  <Field name="size" type="string" default="md">
    Modal size: `sm`, `md`, `lg`, `xl`
  </Field>
  <Field name="closable" type="boolean" default="true">
    Show close button
  </Field>
  <Field name="backdrop" type="boolean" default="true">
    Enable backdrop click to close
  </Field>
</Fields>

**Slots:**
- `default` - Modal content
- `header` - Custom header content
- `actions` - Modal action buttons

---

## Type Definitions

### Command

```typescript
interface Command {
  command: string
  output?: string
  timestamp?: Date
  exitCode?: number
}
```

### Column

```typescript
interface Column {
  key: string
  title: string
  sortable?: boolean
  render?: (value: any, row: any) => string
  width?: string
}
```

### Theme

```typescript
interface Theme {
  name: string
  colors: {
    primary: string
    secondary: string
    background: string
    surface: string
    text: string
    textSecondary: string
  }
  effects: {
    glowIntensity: 'low' | 'medium' | 'high'
    animationSpeed: 'slow' | 'normal' | 'fast'
    particleEffects: boolean
    scanLines: boolean
  }
}
```

---

## Global Props

All components accept these global props:

<Fields>
  <Field name="class" type="string" default="">
    Additional CSS classes
  </Field>
  <Field name="style" type="string" default="">
    Inline CSS styles
  </Field>
  <Field name="id" type="string" default="">
    Element ID
  </Field>
  <Field name="testId" type="string" default="">
    Test identifier for automated testing
  </Field>
</Fields>

---

Need more details on a specific component? Check the individual component documentation pages! 📚⚡